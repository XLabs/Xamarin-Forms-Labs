<?xml version="1.0" encoding="UTF-8" ?>
<mvvm:BaseView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Xamarin.Forms.Labs.Sample.MvvmSamplePage"
    xmlns:mvvm="clr-namespace:Xamarin.Forms.Labs.Mvvm.Views;assembly=Xamarin.Forms.Labs.Mvvm"
    xmlns:labs="clr-namespace:Xamarin.Forms.Labs.Controls;assembly=Xamarin.Forms.Labs"
    Title="MVVM"
    >
    <mvvm:BaseView.Content>
        <StackLayout>
          <Label Text="Does not cache the view" />
        	<Button Text="{Binding NavigateToViewModelButtonText}" Command="{Binding NavigateToViewModel}" />
          <Label Text="Caches the view" />
          <Button Text="{Binding NavigateToKeyedViewModelButtonText}" Command="{Binding NavigateToKeyedViewModel}" />
          <Label Text="Caches the view and uses PushModal" />
          <Button Text="{Binding NavigateModallyText}" Command="{Binding NavigateModallyToViewModel}" />
        </StackLayout>
    </mvvm:BaseView.Content>
</mvvm:BaseView>

